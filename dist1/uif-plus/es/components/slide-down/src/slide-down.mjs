import { prop } from '@uif-plus/utils';

const slideDownProps = {
  modelValue: prop.boolean(),
  transition: prop.boolean(true)
};
const slideDownEmit = [
  "after"
];

export { slideDownEmit, slideDownProps };
//# sourceMappingURL=slide-down.mjs.map
